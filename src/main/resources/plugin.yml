name: LZBR
description: $description
version: '${version}'

main: com.github.Glatinis.lZBR.core.LZBR
api-version: '26.1.2'
load: POSTWORLD
softdepend: [Multiverse-Core, packetevents]

commands:
  lzbr:
    usage: /<command> <join | start>
    alias: [lz]

authors: [ Glatinis ]
